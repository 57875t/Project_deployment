# QR Desk v0.8.0 Audit

Workflow: Preview → Execute → Nitpick → Review → Acceptance → Closure.

## Preview

**Target:** K-line truth core, rise/amplitude metrics, stream-ready transport, and strict reference-source downgrade.

**Boundaries:** no deployment work in this phase; no API keys; no trading; no claim of live acceptance.

Status: `PASS`

## Execute

Implemented in branch `feat/qrdesk-market-truth-v0.8.0`:

- v0.8 Market Truth panel;
- 0–100 truth score;
- latency, recency, gap, duplicate, divergence, and bar-state diagnostics;
- open-to-latest rise, intraday amplitude, and quote-to-bar delta;
- old localhost gateway migration / clearing;
- only HTTPS gateway endpoints;
- public Yahoo/Eastmoney sources reclassified as reference-only;
- reference data blocked from trusted snapshot cache and future prediction;
- forming bars separated from closed bars;
- optional WSS client activated only by advertised gateway capability;
- REST backfill remains active after stream connection.

Status: `PASS`

## Nitpick

| Check | Verdict | Notes |
|---|---|---|
| Simulation written into real fields | PASS | Simulation remains separately labeled. |
| Public source presented as real | PASS | Reclassified to `reference_yahoo` / `reference_eastmoney`. |
| Reference source cached as trusted snapshot | PASS | Cache gate now requires authorized real source. |
| Forming bar overwrites closed history | PASS | Forming bar is isolated. |
| WebSocket opens without declared capability | PASS | Capability gate required. |
| WebSocket bar accepts invalid OHLCV | PASS | Invalid bars are discarded. |
| Old `127.0.0.1:8787` persists in v0.8 | PASS | Legacy local endpoint is cleared. |
| Exchange holiday coverage beyond embedded calendar year | OPEN | A maintained exchange-calendar service is still required. |
| Corporate-action reconciliation across providers | OPEN | Gateway must expose adjustment / split metadata. |
| Cross-provider real-time reconciliation | OPEN | Requires at least two licensed feeds. |
| True live US/HK/CN acceptance | BLOCKED | Requires deployed authorized provider credentials. |

## Review

Static checks completed:

- HTML parsed successfully;
- required v0.8 IDs found;
- JavaScript extracted and passed `node --check`;
- Market Lab rendered in headless Chromium using static content;
- no provider key was added;
- no deployment setting was changed.

Status: `PASS` for static implementation.

## Acceptance

### Passed

- v0.8 HTML artifact produced;
- truth panel renders in blocked state before a real gateway exists;
- gateway field defaults empty;
- source classifications and prediction gates are enforced in code;
- syntax and structural checks pass.

### Not yet passed

- live provider connection;
- measured end-to-end latency;
- live WebSocket reconnect/backfill test;
- US/HK/CN real data samples;
- holiday and half-day verification from a maintained calendar;
- split/dividend adjustment reconciliation.

Overall verdict: `NEEDS FIX / LIVE ACCEPTANCE BLOCKED`.

## Closure

The v0.8 front-end truth core is complete for this phase. Do not label the system “live real-time” until the blocked acceptance items are executed by a deployed authorized gateway.
